{
  "name": "masaar",
  "private": true,
  "version": "0.1.0",
  "description": "Masaar (مسار) — bilingual contractor & tender management platform for SCPP Rev 1.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p packages/working-days && tsc --noEmit -p packages/scpp-rules && tsc --noEmit -p packages/ui && tsc --noEmit -p apps/web",
    "build": "npm run build -w @masaar/web",
    "dev": "npm run dev -w @masaar/web"
  },
  "devDependencies": {
    "@testing-library/react": "^16.1.0",
    "jsdom": "^25.0.1",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  }
}
